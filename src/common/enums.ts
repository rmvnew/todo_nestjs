export enum SortingType {
    ID = 'ID',
    NAME = 'NAME',
    DATE = 'DATE'
}

export enum Typepriority{
    LOW = 'LOW',
    MIDDLE = 'MIDDLE',
    HIGH = 'HIGH'
}

export enum StatusTasks{
    TO_DO = 'TO_DO',
    IN_PROGRESS = 'IN_PROGRESS',
    DONE = 'DONE'
}